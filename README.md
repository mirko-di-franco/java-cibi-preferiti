# Java-Cibi-Preferiti
## Consegna
- creare un `array` con un numero specifico di elementi
- stampare in *console*:
    - la lunghezza dell'array
    - il primo elemento
    - l'ultimo elemento
- eseguire un controllo sull'array per stampare in console gli elementi che si trovano nel mezzo
